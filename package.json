{
	"name": "@edazpotato/simple-blog",
	"version": "0.0.1",
	"description": "Epic gangsta blog",
	"main": "dist/server.js",
	"scripts": {
		"prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
		"build": "tsc",
		"prestart": "npm run build",
		"start": "node .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Edazpotato (https://github.com/edazpotato)",
	"license": "Apache-2.0",
	"dependencies": {
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"express-formidable": "^1.2.0"
	},
	"devDependencies": {
		"@types/dotenv": "^8.2.0",
		"@types/express": "^4.17.11",
		"@types/express-formidable": "^1.0.4",
		"@types/node": "^14.14.22",
		"tslint": "^6.1.3",
		"typescript": "^4.1.3"
	}
}
